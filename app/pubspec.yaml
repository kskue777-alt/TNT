name: tnt_app
description: Placeholder Flutter app configuration for dependency update.

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  home_widget: ^0.5.0

flutter:
  uses-material-design: true
